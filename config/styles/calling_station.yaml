name: "CallingStation"
display_name: "Calling Station"
description: "Rarely folds or raises, just calls everything to see what happens"
preflop_vpip: 0.60
preflop_pfr: 0.05
open_threshold: 0.30
call_threshold: 0.15
reraise_threshold: 0.40
defend_bb_threshold: 0.10

preflop:
  fold_weight: 0.5
  check_weight: 1.0
  call_weight: 4.0
  bet_weight: 0.2
  raise_weight: 0.1
  all_in_weight: 0.1
  bluff_frequency: 0.00
  cbet_frequency: 0.0

flop:
  fold_weight: 0.5
  check_weight: 2.0
  call_weight: 4.0
  bet_weight: 0.2
  raise_weight: 0.1
  all_in_weight: 0.1
  bluff_frequency: 0.00
  cbet_frequency: 0.20

turn:
  fold_weight: 0.5
  check_weight: 2.0
  call_weight: 4.0
  bet_weight: 0.2
  raise_weight: 0.1
  all_in_weight: 0.1
  bluff_frequency: 0.00
  cbet_frequency: 0.15

river:
  fold_weight: 0.5
  check_weight: 2.0
  call_weight: 4.0
  bet_weight: 0.2
  raise_weight: 0.1
  all_in_weight: 0.1
  bluff_frequency: 0.00
  cbet_frequency: 0.0

small_bet_pct: 0.20
medium_bet_pct: 0.30
large_bet_pct: 0.40
